version: "3.8"
services:
  meeting-server:
    build:
      context: ..
      dockerfile: model-docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ../local-data:/app/local-data
    restart: always
